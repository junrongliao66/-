# 小程序发布配置说明

- 已配置 AppID：`wx272b2f43565943d9`。
- `AppSecret`（小程序密钥）仅用于服务端调用微信开放接口，**不要写入小程序前端代码或提交到仓库**。

## 发布建议
1. 在微信公众平台后台管理密钥并按需重置。
2. 若需要调用依赖 `AppSecret` 的接口，请在你自己的后端服务中使用环境变量保存密钥。
3. 代码仓库避免出现明文密钥（包括 `project.config.json`、页面 JS、日志文件）。
